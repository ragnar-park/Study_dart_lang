void main() {
  // 정확한 위치에 어떤 타입의 값이 입력될지 지정할 수 이음
  // (String, int)는 첫 번째 값은 String 타입이고 두 번째 값은 int 타입이다
  (String, int) ronaldo = ('호날두', 7);

  print(ronaldo);
}

// 레코드는 다트 3.0이상부터 사용 가능한 새로운 타입
// 포지셔널 파라미터로 표시한 타입 순서를 반드시 지켜야 함