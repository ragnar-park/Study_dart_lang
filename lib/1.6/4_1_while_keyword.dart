void main(){
  int total = 0;

  while(total < 10){  // total 값이 10보다 작으면 계속 실행
    total += 1;
  }

  print(total);
}
// for문과 마찬가지로 반복적인 작업을 실행할 때 사용
// for문은 호ㅟㅅ수 기반으로 함수를 반복적으로 실행
// while문은 조건을 기반으로 반복문을 실행함
// 조건이 true이면 계속 실행, false이면 멈추게 됨