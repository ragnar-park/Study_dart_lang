void main(){
  var name = 'ragnar';
  print(name);

  // 변숫값 변경 가능
  name = 'ronaldo';

  print(name);

  // 변수명 중복은 불가능 (재할당 불가능)
  // 주석 제거시 에러 발생
  // var name = 'messi';
  //  name = 10;

  // var 타입은 변수의 값을 사용해서 변수의 타입을 유추하는 키워드
  // 타입을 한 번 유추하면 추론된 타입이 고정됨
}