// 고정된 매개변수
int addTwoNumbers(int a, int b) {
  return a + b;
}

void main() {

  print(addTwoNumbers(10, 20));

  // 함수를 사용하면 한 번만 작성하고 여러 곳에서 재활용 가능
  // 다트 함수에서 매개변수를 지정하는 방법
  // 1. 순서가 고정된 매개변수 파라미터와
  // 2. 이름이 있는 매개변수가 았음

}